export enum SortableFields {
    CREATED_AT = 'createdAt',
    UPDATED_AT = 'updatedAt',
    FULL_NAME = 'fullName',
    EMAIL = 'email',
    PHONE_NUMBER = 'phoneNumber',
    STATUS = 'status',
    LAST_LOGIN_AT = 'lastLoginAt',
    ROLE = 'role',
    RANK = 'rank',
}

export enum SortDirection {
    ASC = 'asc',
    DESC = 'desc',
}

export enum SortAuth {
    LOCAL = 'local',
    GOOGLE = 'google',
    FACEBOOK = 'facebook',
}