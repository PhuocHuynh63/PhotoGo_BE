export enum VendorStatus {
  ACTIVE = 'hoạt động',
  INACTIVE = 'không hoạt động',
  SUSPENDED = 'tạm ngưng',
}

export enum VendorManagerRole {
  OWNER = 'owner',
  MANAGER = 'manager',
  STAFF = 'staff',
}